apiVersion: v1
kind: ConfigMap
metadata:
  name: orchestrator-config
data:
  REDIS_URL: redis://redis:6379/0
  JOB_STREAM: jobs
  CONSUMER_GROUP: consumers
  STREAM_MAXLEN: "1000"
  JOBS_PER_SECOND: "1"
  CLAIM_IDLE_MS: "30000"
  CLAIM_INTERVAL_S: "5"
